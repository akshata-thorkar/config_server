# service registers under this name
spring:
  application:
    name: service2

#discover ser
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:1111/eureka/

server:
  port: 3333
